# AI-Native User Flow · Juno

_Module 4. Generated by `M4 - AI User Flow Architect.html`. Paste over `04-ai-ux/user-flow.md`._

## Entry point

**Signal type:** Meeting / call ended

A new Zoom recordings and transcripts are generated on the Zoom website or the summary email reaching to mailbox.

**What they see instantly**

"Juno is reading your meeting transcript…" status pill appears under the input column. Empty Insights and PRD columns dim to ~40% to signal the agent is working.

## The flow

1. 1. Long Context read through the Strategy One-Pager (M3 KB), top-K = 10.
2. Comparison logic, does the transcript pain point map to a strategic pillar?
3. Risk + alignment scoring, emit P0-P3 with a strategic-rationale citation.
4. Confidence check, score < 30 → notRecommended; score ≥ 70 → P0/P1.
2. "Scanning Strategy One-Pager…" → "Cross-referencing 1 transcript with 4 strategic pillars…" → "Synthesising priorities + drafting PRD section…"
3. Path A (Strategy loaded) → grounded prioritization with citations.
Path B (Strategy missing) → "Cautious mode", generic priorities tagged "low confidence" + nudge to load the strategy doc.

## AI moments

**Placement:** Inline & Embedded

Two Insight Cards and a footnote in column 2 (Insights), each with: P0-P3 badge as the priority along with evidence quote from the transcript, and a Strategic Traceability footer citing the strategic pillar. Footnote at bottom to state how many undefined with a link to those TBD items.

PRD draft in column 3 references the cards.

Value prop = augmentation (M2). The PM is the validator, they edit and approve, not create from blank. Inline keeps them in flow with the raw transcript on the left.

## Fallbacks

**Kill switch**

Every Insight Card has a "Manual Override" button → re-tag P0-P3, edit the rationale, or mark notRecommended. PRD blocks are individually editable + regenerable.

**Training signal**

Manual demote → logged as "strategic-alignment correction." 3+ similar overrides → flag the strategic pillar as ambiguous + tighten retrieval.

**Fail-safe**

If Long Context cannot categorize into any strategic priority → Juno tags the card "Out-of scope current strategy" + amber warning. Never invents an alignment. PM should review and can promote manually or send back for clarification.

## Self-review

- [v] Trigger fires on the earliest possible signal, no manual “Start AI” click.
- [v] At least one breadcrumb message turns latency into transparency.
- [v] Maneuver matches the M2 value prop (Automation / Augmentation / Insights / Personalization).
- [v] Every automated decision has a working kill switch.
- [v] Fail-safe path is explicit. No dead end with a bad AI result.
- [v] Hidden logic references M3 PRD specs (Top-K, latency target, knowledge base).
