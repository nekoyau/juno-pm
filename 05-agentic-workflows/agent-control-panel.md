# Agent Control Panel · Juno

_Module 5. Generated by `M5 - Agent Control Panel.html`. Paste over `05-agentic-workflows/agent-control-panel.md`._

## Autonomy level

Agent can draft a P0 strategic priority list. Agent CANNOT delete threads, auto-close threads, tag customers' names or DM customers.

## Controls

- **Kill switch:** max_steps: 5. Abort if same tool fails 2x in a row. Hard timeout: 90s wall clock.
- **Rate / cost caps:** usecase.retrieve to {chunks:[{text,source,score}], summary, confidence}. strategy.lookup_arr to {strategy_name, priority_num}.
- **Escalate-on-stuck:** After 3 failed retrievals, degrade to "cautious mode" (no priorities, just thread links). After 2 tool errors, escalate to PM with full trace.

## Monitoring

**Confidence thresholds (map to actions):**

Score 80%+ to auto-post to #project_smes. 70 to 79% to post to #pm-review with @attention-pm. Below 70% to require human PM approval.

**Checkpoints:**

Any thread mentioning "confidential", "sensitive", "legal", or "security" requires PM approval. Any P0 with confidence below 70% goes to PM review.

**North Star (re-read every loop):**

You are Juno. Your single goal is to surface the top-3 strategic priorities from #project_smes every Monday morning in HKT. Always cite a strategic pillar. Never invent customer names and thread post date. Escalate ambiguity to the PM.

## Permissions

READ: Slack #project_smes, Strategy KB. WRITE: #project_smes or #pm-review only. CANNOT edit Slack threads or post outside #project_smes and #pm-review.

## Self-review

- [ ] Stop conditions include max_steps + wall-clock timeout.
- [ ] Tool outputs include a confidence/score field per retrieval tool.
- [ ] Confidence thresholds map to actions, not just labels.
- [ ] North Star is one sentence, re-read every loop.
- [ ] Each rule of engagement names something the agent CANNOT do.
